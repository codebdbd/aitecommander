diff --git a/app/controllers/structure_modules/operations/category_operations.py b/app/controllers/structure_modules/operations/category_operations.py
index existing..fixed 100644
--- a/app/controllers/structure_modules/operations/category_operations.py
+++ b/app/controllers/structure_modules/operations/category_operations.py
@@ -29,11 +29,11 @@ except ModuleNotFoundError:
 
 if _normalization_module and hasattr(_normalization_module, "validate_normalized_data"):
 
     def validate_normalized_data(
         items: list[dict[str, Any]], *, required_keys: list[str] | None = None
     ) -> bool:
-        return _normalization_module.validate_normalized_data(
+        return _normalization_module.validate_normalized_data(  # type: ignore[union-attr]
             items, required_keys=required_keys
         )
 
 else:

diff --git a/app/views/main_components/ui/window_ui_setup.py b/app/views/main_components/ui/window_ui_setup.py
index existing..fixed 100644
--- a/app/views/main_components/ui/window_ui_setup.py
+++ b/app/views/main_components/ui/window_ui_setup.py
@@ -270,7 +270,7 @@ class WindowUISetup:
         )
         self.window.setCentralWidget(central)
 
-        self.main_layout = QVBoxLayout(central)
+        self.main_layout: QVBoxLayout = QVBoxLayout(central)
         self.main_layout.setContentsMargins(*app_config.ui.get_main_layout_margins())
         self.main_layout.setSpacing(app_config.ui.get_main_layout_spacing())
         try:

diff --git a/app/utils/ui/menu_builders/links_menu_builder.py b/app/utils/ui/menu_builders/links_menu_builder.py
index existing..fixed 100644
--- a/app/utils/ui/menu_builders/links_menu_builder.py
+++ b/app/utils/ui/menu_builders/links_menu_builder.py
@@ -328,7 +328,7 @@ class LinksMenuBuilder:
             from PyQt6.QtWidgets import QApplication
 
             app = QApplication.instance()
-            if app:
-                clipboard = app.clipboard()
+            if app is not None:
+                clipboard = app.clipboard()  # type: ignore[attr-defined]
                 if clipboard:
                     clipboard.setText(url)

diff --git a/app/views/windows/dialogs/entity_dialogs.py b/app/views/windows/dialogs/entity_dialogs.py
index existing..fixed 100644
--- a/app/views/windows/dialogs/entity_dialogs.py
+++ b/app/views/windows/dialogs/entity_dialogs.py
@@ -887,7 +887,7 @@ class BrowserProfileSelectionDialog(QDialog):
 
     def accept(self) -> None:
         """Override to collect selected profiles."""
-        self.result = [cb.profile for cb in self.profile_checkboxes if cb.isChecked()]
+        self.selected_profiles = [cb.profile for cb in self.profile_checkboxes if cb.isChecked()]
         super().accept()
 
     def get_selected_profiles(self) -> list[dict[str, Any]]:
-        return getattr(self, "result", [])
+        return getattr(self, "selected_profiles", [])

diff --git a/app/utils/ui/menu_builders/structure_menu_builder.py b/app/utils/ui/menu_builders/structure_menu_builder.py
index existing..fixed 100644
--- a/app/utils/ui/menu_builders/structure_menu_builder.py
+++ b/app/utils/ui/menu_builders/structure_menu_builder.py
@@ -35,7 +35,7 @@ class StructureMenuBuilder:
         db = getattr(main_window, "db", None)
         if db:
-            self._svc = StructureContextService(db)
+            self._svc = StructureContextService(db)  # type: ignore[arg-type]
         else:
             self._svc = None

diff --git a/app/controllers/ui/menu_controller.py b/app/controllers/ui/menu_controller.py
index existing..fixed 100644
--- a/app/controllers/ui/menu_controller.py
+++ b/app/controllers/ui/menu_controller.py
@@ -41,7 +41,7 @@ class MenuController:
         self.main_window = main_window
         self.logger = logger or logging.getLogger(__name__)
         try:
-            self._main_menu_builder = MainMenuBuilder(self.main_window)
+            self._main_menu_builder = MainMenuBuilder(self.main_window)  # type: ignore[arg-type]
         except Exception as exc:
             self.logger.exception("Failed to create MainMenuBuilder: %s", exc)
             self._main_menu_builder = None
@@ -77,7 +77,7 @@ class MenuController:
         if tree_widget is None:
             return None
         self._structure_menu_builder = StructureMenuBuilder(
-            tree_widget, self.main_window
+            tree_widget, self.main_window  # type: ignore[arg-type]
         )
         return self._structure_menu_builder
 
@@ -88,7 +88,7 @@ class MenuController:
         if table_widget is None:
             return None
         try:
-            self._links_menu_builder = LinksMenuBuilder(table_widget, self.main_window)
+            self._links_menu_builder = LinksMenuBuilder(table_widget, self.main_window)  # type: ignore[arg-type]
         except Exception as exc:
             self.logger.exception("Failed to create LinksMenuBuilder: %s", exc)
             return None
@@ -102,7 +102,7 @@ class MenuController:
         if list_widget is None:
             return None
         self._category_menu_builder = CategoryMenuBuilder(
-            list_widget, self.main_window
+            list_widget, self.main_window  # type: ignore[arg-type]
         )
         return self._category_menu_builder

diff --git a/app/views/widgets/link/base_table.py b/app/views/widgets/link/base_table.py
index existing..fixed 100644
--- a/app/views/widgets/link/base_table.py
+++ b/app/views/widgets/link/base_table.py
@@ -3,7 +3,7 @@ from typing import Any, Optional
 
 from PyQt6.QtCore import QModelIndex, QSize, Qt, pyqtSignal
-from PyQt6.QtCore import QModelIndex, QSize, Qt, pyqtProperty, pyqtSignal
+from PyQt6.QtCore import QModelIndex, QSize, Qt, pyqtSignal
 
 # Remove pyqtProperty import - not available in PyQt6

diff --git a/app/views/widgets/tiles/widget.py b/app/views/widgets/tiles/widget.py
index existing..fixed 100644
--- a/app/views/widgets/tiles/widget.py
+++ b/app/views/widgets/tiles/widget.py
@@ -243,10 +243,10 @@ class TilesWidget(QScrollArea):
             self.updateGeometry()
         except (RuntimeError, AttributeError):
             pass
 
-    def eventFilter(self, obj: QObject, event: QEvent) -> bool:
+    def eventFilter(self, obj: Optional[QObject], event: Optional[QEvent]) -> bool:
         """Handle hover events for tiles."""
-        if isinstance(obj, TileButton):
+        if obj is not None and isinstance(obj, TileButton):
             if event.type() == QEvent.Type.Enter:
                 pos = event.pos()
                 global_pos = obj.mapToGlobal(pos)
@@ -247,7 +247,7 @@ class TilesWidget(QScrollArea):
         """Handle hover events for tiles."""
         if obj is not None and isinstance(obj, TileButton):
             if event.type() == QEvent.Type.Enter:
-                pos = event.pos()
+                pos = event.pos()  # type: ignore[union-attr]
                 global_pos = obj.mapToGlobal(pos)
